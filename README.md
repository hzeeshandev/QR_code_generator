# QR Code Generator

A simple Python script to generate QR codes.

## Installation

Activate your virtual environment and install the required packages:

```bash
pip install qrcode Pillow
```

## How to Use

Run the script using your terminal:

```bash
python qr_code.py
```

This will generate a QR code image file in your folder.
